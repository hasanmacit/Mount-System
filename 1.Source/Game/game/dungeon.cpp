// aratılır:

struct FPurgeSectree

// içinde bulunur

				if (!ch->IsPC() && !ch->IsPet())

// değiştirilir:

#ifdef ENABLE_MOUNT_COSTUME_SYSTEM
				if (!ch->IsPC() && !ch->IsPet() && !ch->IsMount())
#else
				if (!ch->IsPC() && !ch->IsPet())
#endif 



// aratılır:

	struct FKillSectree

// içinde bulunur

				if (!ch->IsPC() && !ch->IsPet())

// değiştirilir:

#ifdef ENABLE_MOUNT_COSTUME_SYSTEM
				if (!ch->IsPC() && !ch->IsPet() && !ch->IsMount())
#else
				if (!ch->IsPC() && !ch->IsPet())
#endif 